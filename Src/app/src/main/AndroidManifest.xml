<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--Please don't remove this permission it is required for notifications to work-->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <!-- So ACTION_SEND / chooser can resolve targets on API 30+ (CSV share). -->
    <queries>
        <intent>
            <action android:name="android.intent.action.SEND" />
            <data android:mimeType="*/*" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="Willow Lotto"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Willow_Lotto_app">


        <!-- App starts here -->


        <meta-data
            android:name="com.google.android.maps.v2.API_KEY"
            android:value="AIzaSyDErr9U0sD14iNjpR2flkx2PkUKkN0mpS4"/>

        <activity

            android:name=".auth.LoginActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>



        <!-- Main screens -->
        <activity android:name=".home.MainActivity" android:exported="false"/>
        <activity android:name=".events.EventsActivity" android:exported="false"/>
        <activity android:name=".notification.NotificationActivity" android:exported="false"/>
        <activity android:name=".profile.ProfileActivity" android:exported="false"/>
        <activity android:name=".organizer.ui.OrganizerDashboardActivity" android:exported="false"/>
        <activity android:name=".organizer.ui.OrganizerMyEventsActivity" android:exported="false"/>
        <activity android:name=".events.CreateEventActivity" android:exported="false"/>
        <activity android:name=".events.EventQrActivity" android:exported="false"/>
        <activity
            android:name=".events.ScanEventQrActivity"
            android:exported="false"
            android:screenOrientation="portrait" />
        <activity android:name=".auth.AdminLoginActivity" android:exported="false"/>
        <!-- Handles deep links like willow-lottery://event/{eventId} -->
        <activity android:name=".events.EventDetailActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:scheme="willow-lottery"
                    android:host="event" />
            </intent-filter>
        </activity>
        <activity android:name=".auth.SignUpActivity" android:exported="false"/>
        <activity android:name=".events.EditEventActivity" android:exported="false"/>
        <activity android:name=".entrant.EntrantMapActivity" android:exported="false"/>
        <activity android:name=".events.InvitedEntrantsActivity" android:exported="false"/>
        <activity android:name=".events.AcceptedEntrantsActivity" android:exported="false"/>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity android:name=".admin.AdminDashboardActivity" />
        <activity android:name=".admin.AdminBrowseProfilesActivity" />
        <activity android:name=".admin.AdminBrowseEventsActivity" />
        <activity android:name=".admin.AdminBrowseImagesActivity" />

    </application>


</manifest>
