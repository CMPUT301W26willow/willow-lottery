<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/backgroundGray">

    <TextView
        android:id="@+id/events_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_marginStart="20dp"
        android:layout_toStartOf="@id/events_scan_qr_btn"
        android:paddingTop="28dp"
        android:paddingBottom="12dp"
        android:text="@string/reg_history_title"
        android:textColor="@color/textDark"
        android:textSize="22sp"
        android:textStyle="bold" />

    <ImageButton
        android:id="@+id/events_scan_qr_btn"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_alignParentEnd="true"
        android:layout_alignTop="@id/events_title"
        android:layout_marginEnd="12dp"
        android:layout_marginTop="20dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="@string/events_scan_qr_content_desc"
        android:padding="12dp"
        android:src="@drawable/ic_qr_scan_24"
        android:tint="@color/primaryPurple" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/events_recycler"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/events_title"
        android:layout_above="@id/bottom_nav"
        android:clipToPadding="false"
        android:paddingTop="4dp"
        android:paddingBottom="16dp" />

    <TextView
        android:id="@+id/events_empty"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:paddingHorizontal="32dp"
        android:text="@string/reg_history_empty"
        android:textAlignment="center"
        android:textColor="@color/textGray"
        android:textSize="16sp"
        android:visibility="gone" />

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_nav"
        android:layout_width="match_parent"
        android:layout_height="88dp"
        android:layout_alignParentBottom="true"
        android:background="@android:color/white"
        android:paddingTop="10dp"
        android:paddingBottom="10dp"
        app:itemIconSize="22dp"
        app:itemIconTint="@color/primaryPurple"
        app:itemTextColor="@color/primaryPurple"
        app:menu="@menu/bottom_menu" />

</RelativeLayout>
