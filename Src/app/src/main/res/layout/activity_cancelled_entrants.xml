<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/backgroundGray"
    android:padding="24dp">

    <TextView
        android:id="@+id/cancelled_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Cancelled Entrants"
        android:textSize="22sp"
        android:textStyle="bold"
        android:textColor="@color/textDark"/>

    <TextView
        android:id="@+id/cancelled_empty"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/cancelled_title"
        android:layout_marginTop="24dp"
        android:text="No cancelled entrants."
        android:textColor="#666666"
        android:visibility="gone"/>

    <ListView
        android:id="@+id/cancelledListView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/cancelled_title"
        android:layout_above="@id/backButton"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="16dp"/>

    <Button
        android:id="@+id/backButton"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_alignParentBottom="true"
        android:text="Back"
        android:textColor="@android:color/white"
        android:background="@drawable/button_primary_purple"/>

</RelativeLayout>